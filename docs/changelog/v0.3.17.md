# v0.3.17 — .section-headline p 반응형 폭 보완

## 작업한 내역

- `2026-05-17` `src/app/_style/_common.scss` 갱신 — `.section-headline p` 폭을 `width: 680px` 고정에서 `width: 100%; max-width: 680px`로 변경. 좁은 viewport에서 본문이 컨테이너를 넘어 overflow되던 문제 해결. `.section-content`(max-width 1024px) 안에서 자연스럽게 컨테이너 폭을 따라가며 1024px 이상에서만 680px로 제한.

## 다음 버전 예정

- 빌드/브라우저 동작 검증 후 발견된 이슈 수정
- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글)
- /projects/eum 후속 섹션(Key Changes 비교형 스크럽, UT Results, Final Reflection 등) 추가
- Project Overview/Background 본문 카피 보강 및 시각 요소(이미지·다이어그램) 추가
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
