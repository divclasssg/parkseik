# v0.3.7 — /projects/eum hero R2 비디오 임베드

## 작업한 내역

- `2026-05-17` `src/app/projects/eum/page.js` 수정 — 비어 있던 `.video-wrapper`에 R2 hero/ 폴더의 3종 mp4(`final_prototype_01/02/03.mp4`)를 `<video>` 태그로 세로 스택 임베드. `@/_lib/media`의 `asset()` 헬퍼로 URL 빌드. 인트린식 `width`/`height` 지정으로 CLS 방지. 동작: `autoPlay loop muted playsInline preload="metadata"`. 그룹 `role="group"` + 개별 `aria-label`로 a11y 보강
- `2026-05-17` `src/app/projects/eum/_style/projects.eum.scss` 수정 — `.video-wrapper`에 flex 세로 스택 + gap 규칙 추가. 각 `.video-item--01/02/03`에 소스 해상도 기반 `max-width` 캡과 `aspect-ratio` 적용. 캡 산정 근거: "표시 폭 ≤ 소스 폭 ÷ 2(레티나 DPR)" — 01/02 = 660 CSS px, 03 = 387 CSS px. 레티나(2x)와 일반(1x) 디스플레이 어디서도 픽셀 매핑이 1:1을 넘지 않아 흐려지지 않음

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- prefers-reduced-motion 사용자 대상 autoplay 정지 처리(클라이언트 훅 도입 여부 함께 검토)
- hero 비디오 poster 이미지 생성/업로드(첫 프레임 LCP 개선)
