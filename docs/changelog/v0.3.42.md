# v0.3.42

## 작업한 내역

### 2026-05-19

- **carousel-slider 실동작 슬라이더 구현**
  - `slideIndex` useState + `SLIDE_COUNT = 3` 추가 (`page.js`)
  - `.slider-wrapper`에 `transform: translateX(-N * 100%)` 인라인 스타일 적용
  - `.slider-nav` 추가: 좌(←)/우(→) 화살표 버튼 (SVG 쉐브론)
  - 첫/마지막 슬라이드에서 각 버튼 `disabled` (opacity 0.2)
  - SCSS: `.carousel-slider` → `overflow: hidden`, `max-width: 1024px`, `margin: 0 auto`
  - SCSS: `.slider-wrapper` → `display: flex` + `transition: transform 0.4s cubic-bezier(...)`
  - SCSS: `.slider-item` → `flex: 0 0 100%`, `min-width: 0` (translateX 슬라이드 방식)
  - SCSS: `.slider-nav`, `.slider-arrow` 버튼 스타일 추가 (40×40 원형, hover/disabled 상태)
