# v0.3.37 — /projects/eum hero phone 프레임 우측 flush

## 작업한 내역

- `2026-05-19` `src/app/projects/eum/_style/projects.eum.scss` — `.phone-frame`의 `right` 값을 `max(48px, calc(...))` 복잡한 계산식에서 `right: 0`으로 단순화.
- `2026-05-19` `src/app/projects/eum/page.js` — Develop 섹션 하단에 `carousel-slider` 스캐폴드 추가 (빈 슬라이더 구조 초안). video-wrapper max-width: 1200px 도입 이후 100vw 기반 계산식이 wrapper 기준과 맞지 않아 무효화됨.

## 배경

v0.3.34에서 phone 프레임 우측을 monitor 우측 끝과 맞추기 위해 `(100vw - monitor_w) / 2` 공식을 사용했으나, v0.3.35에서 video-wrapper에 max-width: 1200px가 적용되고 monitor가 width-driven으로 전환되면서 기준이 달라짐. 사용자 지시에 따라 video-wrapper 우측 끝에 flush하는 `right: 0`으로 단순화.
