# v0.3.29 — 홈·localnav·공통 버튼·card·hero typography 미세 조정

## 작업한 내역

- `2026-05-18` `src/app/page.js` — `.header-home-headline` 안 "Product Designer"를 `<strong>`으로 감쌈 → 강조 처리
- `2026-05-18` `src/app/_style/_home.scss` — `.header-home-headline strong` 스타일 추가: `background: #1d1d1f`, `font-weight: 700`, `color: #fff` (검정 inline 강조)
- `2026-05-18` `src/app/_style/_localnav.scss` — `.localnav-inner`에 공통 `padding: 24px` 추가. `.menu-wrapper`(24px → 0 0 16px), `.content-wrapper`(24px → 24px 0, `position: relative` + `::before` 1px `#e5e5ea` 디바이더 신규), `.contentnav`(0 24px 48px → 0 0 24px) — padding 재구성으로 outer/inner 분리 + content-wrapper 상단 가는 구분선
- `2026-05-18` `src/app/_style/_common.scss` — `.button-primary`/`.button-secondary`/`.button-tertiary` font-weight 400 → 500 통일
- `2026-05-18` `src/app/projects/_style/projects.style.scss` — `.card-content` padding-bottom 36px → 64px. `.card-grid-wrapper > div` padding 48px → 36px. `.button-wrapper` padding-bottom 48px → 36px
- `2026-05-18` `src/app/projects/eum/_style/projects.eum.scss` — `.hero-headline` 그라디언트 종점 70% → 40% + `display: inline-flex`로 박스 크기 보정. `.button-elevated` 그라디언트 `90deg 50%` → `134deg 70%`(대각선 + 깊이 ↑). `.keyscreen-callout-headline` font-size 28px → 24px

## 배경

hero 영상 작업(v0.3.20~v0.3.28)과 병행해 IDE에서 누적된 디자인 미세 조정. 홈 헤더 강조, localnav 내부 padding 구조 정리 + 구분선, 공통 버튼 font-weight 통일, card·hero·keyscreen typography 미세 조정.

## 다음 버전 예정

- card-set-full-bleed 및 card-column-3 이미지 자산 연결
- 04 Deliver 섹션 추가
- localnav 디스클로저 버튼 인터랙션 구현
