# v0.3.13 — /projects/eum 히어로 비디오 배속 + localnav 메뉴 정렬

## 작업한 내역

- `2026-05-17` `src/app/projects/eum/page.js` 수정 — `VIDEOS` 배열 각 항목에 `rate` 필드 추가(`01: 10`, `02: 10`, `03: 5`)하여 데모 시연 호흡을 단축. 활성 비디오 useEffect에서 `play()` 직전 `v.playbackRate = VIDEOS[i].rate ?? 1.0`을 재적용 — 일부 브라우저가 `src` 마운트나 `currentTime` 변경 시점에 `playbackRate`를 1.0으로 리셋하는 케이스가 있어, `currentTime = 0` 직후·`play()` 직전에 매번 다시 세팅해 매 사이클·매 활성 전환에서 배속 보존 보장. 비디오는 모두 `muted`라 고배속에서도 오디오 노이즈/피치 이슈 없음
- `2026-05-17` `src/app/projects/eum/page.js` 포매팅 정리 — `<div className="video-wrapper">` 오프닝 태그, `<div className="monitor-frame__screen">` 래퍼, `<p className="hero-violator">` 본문을 Prettier 한 줄 폼으로 압축(가독성·diff 노이즈 감소). 마크업 의미 변경 없음
- `2026-05-17` `src/app/_style/_localnav.scss` 수정 — `.menu-wrapper`에 `align-items: center` 추가. 기존 `display: flex; justify-content: space-between`만으로는 디스클로저 버튼과 텍스트가 cross-axis 기준선이 어긋나(버튼 박스가 텍스트보다 커서 위쪽 정렬) 시각적 불균형 발생. center 정렬로 두 자식 수직 중앙 일치

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- prefers-reduced-motion 사용자 대상 자동 전환 정지 + 배속 1.0 강제 처리
- hero 비디오 poster 이미지 생성/업로드(첫 프레임 LCP 개선)
- hero padding(64/96)을 viewport 크기에 따라 조정해 작은 viewport에서 비디오 영역 확보
