# v0.3.3 — 웹표준·시맨틱·접근성 정비

## 작업한 내역

- `2026-05-16` `src/app/_components/localnav.js` 신규 — Eum 프로젝트 페이지용 로컬 네비게이션 컴포넌트(접힘/펼침 wrapper, CTA 버튼, 사이트 메뉴 링크) 추가. 작성 단계부터 시맨틱·접근성 적용: `rel="noopener noreferrer"` 외부 링크 보안, 외부/내부 `<nav>`에 각각 `aria-label="현재 프로젝트"`·`aria-label="사이트 메뉴"`, 디스클로저 `<button>`에 `aria-expanded="false"`·`aria-controls="localnav-expanded"` + 대응 wrapper에 `id="localnav-expanded"`, 페이지 `<h1>`과 중복을 피해 프로젝트 라벨은 평문 `<p>`로 표기, 내부 링크 `target="_self"` 미부여
- `2026-05-16` `src/app/projects/eum/layout.js` 수정 — Localnav 컴포넌트 import 후 children 위에 렌더링하도록 레이아웃 연결
- `2026-05-16` `src/app/layout.js` 수정 — `<body>` 첫 자식으로 `<a className="skip-to-content" href="#main-content">본문 바로가기</a>` skip-link 추가 (visually-hidden brand `<h1>`은 유지)
- `2026-05-16` `src/app/_components/globalnav.js` 수정 — `<nav>`에 `aria-label="전역 메뉴"` 부여, 내부 링크 `target="_self"` 제거
- `2026-05-16` `src/app/page.js` 수정 — `<main>`에 `id="main-content"` 부여, `<nav>`에 `aria-label="주요 섹션"` 부여, 내부 링크 `target="_self"` 제거
- `2026-05-16` `src/app/projects/page.js` 수정 — `<main>`에 `id="main-content"` 부여, `<nav>`에 `aria-label="프로젝트 목록"` 부여, 내부 링크 `target="_self"` 제거
- `2026-05-16` `src/app/research/page.js` 수정 — `<main>`에 `id="main-content"` 부여, `<nav>`에 `aria-label="리서치 목록"` 부여, 내부 링크 `target="_self"` 제거
- `2026-05-16` `src/app/about/page.js` 수정 — `<main>`에 `id="main-content"` 부여
- `2026-05-16` `src/app/projects/eum/page.js` 수정 — `<main>`에 `id="main-content"` 부여

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현 (클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글)
- 페이지 본문 콘텐츠 작성 시 about 페이지의 `<header>` 시맨틱 재검토
