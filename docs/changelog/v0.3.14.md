# v0.3.14 — 클래스명 BEM 제거 + flat 대시 컨벤션 명문화

## 작업한 내역

- `2026-05-17` `src/app/projects/eum/page.js`, `src/app/projects/eum/_style/projects.eum.scss` 리팩토링 — `/projects/eum`에 잔존하던 BEM 패턴(`block__element`, `block--modifier`)을 모두 단일 대시 컨벤션으로 평탄화. 변경 매핑:
    - `phone-frame__bezel` → `phone-frame-bezel`
    - `monitor-frame__screen` → `monitor-frame-screen`
    - `phone-frame--01/03` → `phone-frame-01/03` (SCSS `&--01` → `&-01`)
    - `monitor-frame--02` → `monitor-frame-02` (JSX 템플릿만, 대응 SCSS 규칙 없음)
    - `video-item--01/02/03` → `video-item-01/02/03`
    - `is-active` → `active` (state 접두사 제거, 부모 셀렉터 `.phone-frame.active`로 scope)
    - JSX 5곳·SCSS 5곳·SCSS 주석 1곳 일괄 치환
    - `#__next`(Next.js 예약 ID)는 예외 유지
    - 동작·스타일·레이아웃 변경 없음 — 순수 텍스트 치환. grep으로 잔존 BEM/`is-active` 0건 확인
- `2026-05-17` `CLAUDE.md` 업데이트 — "클래스명 명명 규칙" 섹션 신설. 단일 대시 통일·BEM 금지·state는 `is-` 없이 와일드 단어(`active`, `open`)·variant 인덱스 정책·`#__next` 예외를 명문화하여 향후 새 클래스 도입 시 결정 비용 제거

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- prefers-reduced-motion 사용자 대상 자동 전환 정지 + 배속 1.0 강제 처리
- hero 비디오 poster 이미지 생성/업로드(첫 프레임 LCP 개선)
- hero padding(64/96)을 viewport 크기에 따라 조정해 작은 viewport에서 비디오 영역 확보
