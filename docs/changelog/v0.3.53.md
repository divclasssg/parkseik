# v0.3.53 — /projects/eum carousel-slider Prototype Key Screens 3장 이미지 임베드

## 작업한 내역

- `2026-05-20` `src/app/projects/eum/page.js`
    - `<h3 className="visuallyhidden">Prototype Key Screens</h3>` 추가 (`.carousel-slider` 직전) — portfolio a11y 패턴 매칭.
    - 3개 슬라이드의 빈 `<div className="slider-visual"></div>`를 `<picture><source srcSet="...1x" type="image/png"><img></picture>` 마크업으로 채움. portfolio `_data/wireframeKeyScreens.js` 매핑을 R2 path 그대로 가져옴.
        - Key Screen 01 → `eum/screenshots/develop/img-keyscreens-01_hodl0f.png` (1296×474)
        - Key Screen 02 → `eum/screenshots/develop/img-keyscreens-02_te2jdq.png` (1296×474)
        - Key Screen 03 → `eum/screenshots/develop/img-keyscreens-03_dvgeex.png` (1296×474)
- `2026-05-20` `src/app/projects/_style/projects.style.scss` — `.carousel-slider .slider-wrapper .slider-item` 블록 안에 `.slider-visual` 규칙 신규 추가. `max-width: 648px; margin: 24px auto 0`로 중앙 정렬 + `picture/img` 사이즈 룰 + `border-radius: 12px` 시각 정돈.

## 배경

`/projects/eum`의 `section-dd-develop-wireframe-to-prototype` 섹션 캐러셀은 portfolio `sectionDevelopWireframe.js`의 동일 구조와 텍스트(eyebrow/headline/subhead/steps 1:1 일치)를 가지고 있지만 `.slider-visual` 영역만 비어 있었음. R2 origin이 동일해 portfolio의 wireframeKeyScreens 이미지 경로 그대로 재사용.

## Retina 정합

소스 1296×474 → 표시 max-width 648 = 정확히 소스÷2. DPR=2 retina에서 1:1 매핑 = 완벽 sharp. memory `feedback_retina_media_cap` 룰을 보수적으로 만족 (Develop card-tile 작업과 같은 패턴, 트레이드오프 명시 불필요).

슬라이더 카드 컨텐츠 영역(~976px)보다 이미지가 작아 좌·우 여백이 생기지만, `margin: 0 auto`로 중앙 정렬해 와이드 카드 안에서 균형 유지.

## a11y

`<h3 className="visuallyhidden">Prototype Key Screens</h3>` 추가로 스크린리더가 캐러셀의 시맨틱 그룹을 인식. 각 이미지 alt도 portfolio 데이터 그대로 ("증상 기록 키스크린 캡쳐본" / "의사 대시보드 키스크린 캡쳐본" / "환자 진료 요약 키스크린 캡쳐본").

## 일관성

자산 작업 시리즈(v0.3.48~52)와 동일한 native `<picture><source srcSet="...1x" type="image/png"><img loading="lazy" decoding="async" width height alt /></picture>` 컨벤션 유지. 후속 2x 자산 합류 시 srcSet `, <2x> 2x` 슬롯 채움.

## Follow-up (별도)

- 2x R2 자산 업로드 후 srcSet 확장 — 현 cap이 정확히 소스÷2라 retina 추가 이득은 작음.
- slider-visual을 row 레이아웃(좌측 텍스트 / 우측 이미지)으로 재구성 검토 — 현재는 column block stack.
- 캐러셀 자동 재생 / keyboard 네비게이션 등 인터랙션 보강.
