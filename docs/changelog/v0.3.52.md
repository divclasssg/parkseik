# v0.3.52 — /projects/eum Develop 방법론 카드 3장 process 스크린샷 임베드

## 작업한 내역

- `2026-05-20` `src/app/projects/eum/page.js` — Develop 섹션 `card.card-tile` 3장(브레인스토밍 / MoSCoW / 최종 MVP 요약)의 빈 `<picture></picture>`를 native `<picture><source srcSet="...1x" type="image/png"><img></picture>` 마크업으로 채움. portfolio `_data/developProcess.js` 매핑을 R2 path 그대로 가져옴.
    - 브레인스토밍 → `eum/screenshots/develop/develop-method-brainstorming_dfkavp.png` (656×912)
    - MoSCoW → `eum/screenshots/develop/develop-method-moscow_ii0qvn.png` (656×912)
    - 최종 MVP 요약 → `eum/screenshots/develop/develop-method-mvp_lsl3nn.png` (656×912)
- `2026-05-20` `src/app/projects/_style/projects.style.scss` — `&.card-tile .card-media` 블록에 `picture { width: 328px; height: 456px }` + `img { width: 100%; height: 100%; object-fit: cover }` 추가. 표시 328×456 = 소스 656×912 ÷ 2 → memory `feedback_retina_media_cap` 룰 정확히 일치.

## 배경

portfolio `_components/sectionDevelop.js`의 `.card-process .card-process-screenshot`에는 Develop 단계 3개 방법론(브레인스토밍 / MoSCoW / MVP) 자료가 R2에 업로드되어 있음. parkseik/main `/projects/eum` Develop 섹션의 `card-group col-3` 안 3개 `card.card-tile`은 이미 portfolio의 `.card-process-screenshot`과 동일한 컨테이너 스타일(border-radius 24, 회색 surface, height 512, 우하단 정렬)을 가졌고 picture만 비어 있는 상태였음. 동일 R2 origin이므로 path 그대로 재사용 가능 → markup + img sizing 룰만 추가.

## 매핑

| `page.js` line | 카드 | R2 path | 원본 W×H | 표시 W×H |
| --- | --- | --- | --- | --- |
| 589–610 | 브레인스토밍. | `eum/screenshots/develop/develop-method-brainstorming_dfkavp.png` | 656×912 | 328×456 |
| 612–633 | MoSCoW. | `eum/screenshots/develop/develop-method-moscow_ii0qvn.png` | 656×912 | 328×456 |
| 635–656 | 최종 MVP 요약. | `eum/screenshots/develop/develop-method-mvp_lsl3nn.png` | 656×912 | 328×456 |

라인 번호는 markup 확장 후 기준.

## Retina 정합

소스 656×912 → 표시 328×456 = 정확히 소스÷2. DPR=2 retina에서 1050 device px와 1:1 매핑 = 완벽 sharp. memory `feedback_retina_media_cap` 룰을 가장 보수적으로 만족하는 케이스 (Discover/Define처럼 cap 완화·트레이드오프 명시가 필요 없음).

## 일관성

Discover(v0.3.48–49) · Define(v0.3.50–51) 자산 작업과 동일한 패턴 유지:
- native `<picture><source srcSet="<path> 1x" type="image/png"><img src loading="lazy" decoding="async" width height /></picture>`
- 후속 2x 자산 합류 시 `srcSet`에 `, <2x> 2x` 슬롯 채움
- alt 텍스트는 portfolio 데이터의 `image.alt` 그대로

## Follow-up (별도)

- 2x R2 자산 업로드 후 srcSet 확장 — 단 현 cap이 정확히 소스÷2라 retina 추가 이득은 작음. 표시 영역을 확대(예: 400×556)로 키울 때만 의미 있음.
- card-group col-3가 768px 이하 뷰포트에서 1열 stack으로 자연 전환되는지 확인 — 별도 필요 시 미디어쿼리 추가.
