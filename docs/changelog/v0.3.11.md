# v0.3.11 — /projects/eum hero 비디오 순차 재생 + 레이어 스택 + 100svh 제한

## 작업한 내역

- `2026-05-17` `src/app/projects/eum/page.js` 수정 — 페이지를 **client component**(`"use client"`)로 전환. `VIDEOS` 배열로 3개 비디오 메타 선언, `useRef` 3개 + `useState(active)` + `useEffect` 두 개로 순차 재생 로직 구현:
    - effect #1(`[]`): 각 비디오에 `ended` 리스너 → `setActive((i+1) % 3)`로 다음 비디오 활성화
    - effect #2(`[active]`): 활성 비디오는 `currentTime = 0` 후 `play()`, 나머지는 `pause()`
    - 활성 프레임에 `is-active` 클래스 + 비활성에 `aria-hidden="true"`
    - 모든 `<video>`에서 `autoPlay`·`loop` 제거(수동 제어). `muted playsInline preload="metadata"` 유지로 모바일 인라인 자동 재생 정책 준수
    - cleanup으로 리스너 해제(메모리 누수 방지)
- `2026-05-17` `src/app/projects/eum/_style/projects.eum.scss` 수정 — `.video-wrapper` 레이아웃 전면 재구성:
    - flex 세로 스택 제거. `position: relative; height: calc(100svh - 180px); margin: 0 auto 32px`로 변경(글로벌/로컬 nav + section-content 공간 확보, section-content와 32px 간격)
    - `.phone-frame`·`.monitor-frame` 공통 규칙: `position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); height: 100%; width: auto; max-height: 100%`로 중앙 절대 배치 + 높이 기준 fit
    - 페이드 전환: 기본 `opacity: 0; visibility: hidden; transition: opacity 200ms ease`, `.is-active` 시 `opacity: 1; visibility: visible`
    - 기존 베젤 안쪽 비디오 매핑(01·03의 `top: 2.2%; left: 5.102%; width: 89.796%; height: 95.6%`, 02의 padding-기반 베젤) 변경 없음
- `2026-05-17` fix(SCSS) `.monitor-frame .video-item` — height-fit 레이아웃에서 02 비디오 상·하단이 잘리는 증상 발생. 원인: 외곽 `aspect-ratio: 3024/1964` + `padding: 12px`이 함께 적용되면 content-box 비율이 외곽 비율과 ~1% 어긋나는데 `object-fit: cover`가 그 차이만큼 비디오를 잘라냄. → `object-fit: contain` + `background: transparent`로 변경하여 잘림 0, 미세 letterbox는 부모 베젤색(#1d1d1f)에 자연 흡수
- `2026-05-17` fix(JSX+SCSS) `.monitor-frame` 베젤이 비디오에 딱 맞도록 재구성 — 이전 단계에서 잔존하던 (a) `max-width: 1024px`으로 인한 wrapper 상·하단 빈 공간, (b) padding-aspect 불일치로 인한 미세 letterbox 두 가지 모두 제거. 변경: 외곽 `.monitor-frame`에서 `aspect-ratio` 제거 + `max-width: 100%`로 풀어 wrapper 높이까지 차오름. 새로 도입한 `.monitor-frame__screen` 안쪽 div에 `aspect-ratio: 3024/1964; height: 100%; max-width: 1512px(레티나 캡)`을 부여 → content-box가 정확히 비디오 비율 → `object-fit: cover`로 빈 공간 0. `page.js`의 monitor 렌더 분기에 `<div className="monitor-frame__screen">` 래퍼 추가

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- prefers-reduced-motion 사용자 대상 자동 전환 정지 처리
- hero 비디오 poster 이미지 생성/업로드(첫 프레임 LCP 개선)
- 전환 시 다음 비디오 사전 버퍼링(preload 동적 조절) 최적화 검토
