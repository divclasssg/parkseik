# v0.3.38 — /projects/eum hero 영상 visibility 기반 재생 제어

## 작업한 내역

- `2026-05-19` `src/app/projects/eum/page.js` — `IntersectionObserver`로 hero 섹션 노출 여부를 감지해 보이지 않을 때 영상 일시정지, 다시 보이면 resume. `sectionRef`(hero section), `activeRef`(stale closure 방지), `isVisibleRef`(노출 상태) 세 ref 추가. play/pause useEffect에 `isVisibleRef.current` 조건 추가해 화면 밖에서 active 전환 시 play를 억제.

## 배경

hero 영상이 스크롤로 화면에서 벗어나도 계속 재생되어 불필요한 CPU·배터리를 소모함. `threshold: 0` IntersectionObserver로 1px이라도 보이면 재생, 완전히 사라지면 정지.
