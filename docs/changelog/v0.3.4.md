# v0.3.4 — Localnav 스타일링 및 구조 보강

## 작업한 내역

- `2026-05-16` `src/app/_style/_localnav.scss` 신규 — `.localnav` 컴포넌트 스타일 정의 (collapsed/expanded wrapper, `.button-small`/`.button-small-primary`/`.button-small-secondary` 버튼 시스템, `.menu-wrapper`/`.content-action`/`.contentnav` 영역)
- `2026-05-16` `src/app/_style/style.scss` 수정 — `_localnav.scss` `@use` 추가, `subnav.scss`의 확장자 제거(파셜 import 일관성)
- `2026-05-16` `src/app/_components/localnav.js` 수정 — collapsed wrapper 버튼들에 공통 `.button-small` 클래스 시스템 적용, expanded wrapper의 컨테이너 클래스명 정리(`content-expand-wrapper` → `content-expanded-wrapper`, `project-action`/`project-name` → `content-action`/`content-name`), 메뉴 영역에 닫기 `<button>` + `<span className="blind">닫기</span>` 라벨 추가, 프로젝트 목차 `<nav className="contentnav" aria-label="프로젝트 목차">` 신설(앵커 `#overview`/`#background`/`#designProcess`/`#keytakeaway`)

## 다음 버전 예정

- `.blind` 유틸 클래스 SCSS 정의 (현재 마크업에는 사용되나 스타일 미정의 — 화면에 텍스트가 그대로 노출됨)
- localnav 디스클로저 버튼 인터랙션 구현 (클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, 닫기 버튼 동작 연결)
