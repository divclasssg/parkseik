# v0.3.6 — Cloudflare R2 공개 버킷 설정 포팅

## 작업한 내역

- `2026-05-17` `src/_lib/media.js` 신규 — portfolio 프로젝트의 R2 미디어 헬퍼 이식. `R2_ORIGIN`(`https://pub-e26b73e054cf43faa65ef7ee77476e58.r2.dev`), `BASE = ${R2_ORIGIN}/portfolio`, `asset(path)`, `sizes`(full/wide/figure1200/card/fixed), `QUALITY_UI = 90` export. 사용처에서는 항상 `@/_lib/media`로 직접 임포트
- `2026-05-17` `next.config.mjs` 수정 — `images.remotePatterns`에 R2 호스트(`pub-e26b73e054cf43faa65ef7ee77476e58.r2.dev`, `pathname: "/**"`) 추가하여 `next/image` 원격 로딩 허용. 그 외 formats/qualities/deviceSizes/minimumCacheTTL 등은 의도적으로 추가하지 않음(필요 시점에 별도 도입)
- `2026-05-17` `src/app/layout.js` 수정 — `@/_lib/media`에서 `R2_ORIGIN` 임포트, `<head>` 블록 추가 후 R2 origin에 대한 `<link rel="preconnect" crossOrigin="">` / `<link rel="dns-prefetch">` 삽입(이미지·영상 최초 요청 RTT 단축)

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- eum 본문에 R2 미디어(`next/image` + `asset()`) 실제 적용
