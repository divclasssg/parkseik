# v0.3.43

## 작업한 내역

### 2026-05-19

- **carousel-slider peek 슬라이더 구현 (인접 카드 노출)**
  - `translateX` 방식 → `overflow-x: scroll + scroll-snap-type: x mandatory` 전환
  - 아이템 폭 `calc(100vw - 160px)` — 좌우 80px peek 공간 (인접 카드 56px 노출)
  - `.slider-wrapper` → `padding: 0 80px` (첫 아이템 중앙 정렬용)
  - `scrollbar-width: none` + `::-webkit-scrollbar { display: none }` (스크롤바 숨김)
  - `.slider-nav` → `.carousel-slider` 밖으로 이동 (스크롤 컨테이너 내에서 사라지는 문제 해결)
  - `goToSlide(idx)` 함수 추가: `scrollTo({ left: idx * (itemWidth + 24) })` 로 스냅 위치 제어
  - `carouselRef` 추가 (`.carousel-slider` 참조)
