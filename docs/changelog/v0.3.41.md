# v0.3.41

## 작업한 내역

### 2026-05-19

- **carousel-slider 마크업 재설계 (타입 확장성 + 어드민화)**
  - 익명 `<div>` 전체에 역할 기반 클래스명 부여
  - `slider-item` → `slider-item slider-item-steps` (타입 식별자 추가, `card card-bleed` 패턴)
  - `slider-content` → `slider-body` (`card-body` 패턴 일치)
  - 익명 div (eyebrow+headline+subhead) → `slider-intro`
  - 익명 div (dl 래퍼) → `slider-steps`
  - `<dl>` → `slider-step-list`
  - 각 dt+dd 쌍 래퍼 → `slider-step`
  - `slider-image` → `slider-visual` (이미지 외 비디오·스크린샷 등 일반화)
