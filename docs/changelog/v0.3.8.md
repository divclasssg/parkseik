# v0.3.8 — /projects/eum 비디오 01·03에 iPhone 프레임 오버레이

## 작업한 내역

- `2026-05-17` `public/images/iPhone 17 Pro Max - Deep Blue - Portrait.png` 추가 — 1470×3000 RGBA, 스크린 영역 알파 0. PIL 알파 스캔으로 베젤 인셋 측정: top 2.200% / bottom 2.200% / left·right 5.102%. 안쪽 스크린 1320×2868
- `2026-05-17` `src/app/projects/eum/page.js` 수정 — 세로형 비디오 01·03을 `.phone-frame.phone-frame--01/--03` 래퍼로 감싸고 그 안에 베젤 PNG `<img>` 추가(`alt=""` + `aria-hidden="true"`로 장식 처리). 가로형 02는 변경 없음
- `2026-05-17` `src/app/projects/eum/_style/projects.eum.scss` 수정 — 기존 `.video-item--01/--03` max-width/aspect-ratio 규칙 삭제(프레임으로 이관). `.phone-frame`은 `aspect-ratio: 1470/3000`로 PNG 비율 박스 확보, 안쪽 비디오는 베젤 안 스크린 영역에 절대 배치, `border-radius: 4%`로 스크린 라운드 코너 근사. `.phone-frame__bezel`은 `pointer-events: none`로 클릭/탭 방해 없음. 프레임 max-width: 01 = 735 CSS px (안쪽 비디오 660px), 03 = 431 CSS px (안쪽 비디오 387px) → [[feedback-retina-media-cap]] 규칙 그대로 유지
- `2026-05-17` fix(SCSS) `.phone-frame .video-item` — 초기 구현에서 `position: absolute; inset: 2.2% 5.102%; width: auto`만 줬더니 `<video>`가 stretching되지 않고 intrinsic 크기 그대로 표시되어 프레임 밖으로 삐져나가는 증상 발생. 원인: `<video>`는 replaced element라 CSS2 §10.3.8에 따라 `width: auto`가 intrinsic으로 해석됨 → 명시적 `top: 2.2%; left: 5.102%; width: 89.796%; height: 95.6%` 퍼센트로 교체하여 스크린 영역에 정확히 fit

## 다음 버전 예정

- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
- prefers-reduced-motion 사용자 대상 autoplay 정지 처리(클라이언트 훅 도입 여부 함께 검토)
- hero 비디오 poster 이미지 생성/업로드(첫 프레임 LCP 개선)
- 가로형 02도 별도 디바이스 목업(맥북 등) 검토 여부
