# v0.3.15 — Scroll Scrub Pattern 레퍼런스 문서 포팅

## 작업한 내역

- `2026-05-17` `docs/scroll-scrub-pattern.md` 신규 — portfolio repo(`/Users/seikpark/Desktop/portfolio`)의 Apple 스타일 스크롤 스크럽 애니메이션 패턴 문서를 본 repo로 가져와 레퍼런스로 보관. 핵심 내용:
    - HTML 구조(scroll-container + sticky + callout-area + video-track)
    - 3단 타이밍 구간(진입 25% / 고정+스크럽 50% / 퇴출 25%)
    - JS 구현(스크롤 진행률·텍스트 트랜지션·`currentTime` 스크럽·트랙 슬라이드·`requestAnimationFrame` 바인딩)
    - CSS 핵심(`position: sticky`, `mask-image` fade, `will-change` GPU 가속)
    - 영상 URL 패턴은 portfolio 원본(Cloudinary) 대신 본 repo 표준인 Cloudflare R2 사용 안내로 보강
    - 문서 상단에 출처·포팅 날짜·본 repo 컨벤션(단일 대시 클래스명, R2 URL) 적용 주의사항 메타 블록 추가
    - 구현체(컴포넌트·스타일)는 미포팅. 향후 `/projects/eum` 본문 key screens / key changes 섹션 구현 시 이 문서를 참조해 본 repo 컨벤션에 맞춰 재작성 예정
- `2026-05-17` `docs/index.md` 업데이트 — 현재 버전 포인터 v0.3.14 → v0.3.15, 버전 목록 테이블에 v0.3.15 행 추가, 새 섹션 "패턴 문서"를 만들어 `scroll-scrub-pattern.md` 링크 노출

## 다음 버전 예정

- `/projects/eum` 본문 key screens 섹션 구현 (스크롤 스크럽 패턴 첫 적용)
- localnav 디스클로저 버튼 인터랙션 구현(클라이언트 컴포넌트 전환 + `aria-expanded` 동적 토글, `IconClose` 버튼 동작 연결)
- cronometer / liverpoolfc 등 다른 projects variant 스캐폴드 추가
